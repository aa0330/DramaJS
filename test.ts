
let arr = [1, 2, 3];
arr.forEach(ele => {
    ele += 1
})
arr.map(ele => {
    ele += 1;
})

const arr1: number[] = []
arr1.push(1)
console.log(Number.parseInt('0101', 2));





// this , arguments、不能使用new，作为构造函数、不能使用yield命令，作为Generator函数
